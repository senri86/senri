<%= form_for @menu do |f| %>
    <%= f.label :body %>
    <%= f.text_field :body, :size => 140 %> 
    <%= f.submit "編集する" %>
  <% end %>

<%= render 'form', menu: @menu %>

<%= link_to '詳細へ', @menu %> |
<%= link_to '戻る', menus_path %>
